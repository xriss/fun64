cd `dirname $0`

git pull origin master
git checkout master

plated/upgrade

if cd exe; then

	git checkout exe
	git pull
	cd .. 

else git clone https://github.com/xriss/gamecake.git

	cd exe
	git checkout exe
	cd ..

fi

echo " docs worktree to track gh-pages branch "
git branch -f --track gh-pages origin/gh-pages
git worktree add docs gh-pages

